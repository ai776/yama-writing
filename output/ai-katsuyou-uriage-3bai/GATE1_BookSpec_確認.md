# G-1 BookSpec / Strategy / Production 確認

`BookSpec.json`・`StrategySpec.md`・`ProductionSpec.md` を生成済みです。

## 確認してほしい点

1. **表題** `AIと外注で売上の規模をつくる` でよいか（依頼テーマの「3倍」は本文では約束しない方針）。
2. **ペルソナ** 事業責任者〜代理店オーナー想定で問題ないか。
3. **文字数目標** 初版は約1万字前後から拡張。最終5万字目標で追筆するか。

**OK** の場合はそのまま `GATE3`（目次・見出しは既に `toc.md` / `heading_spec.md` あり）へ進めます。修正は箇条書きで返信ください。
